INSERT
    OR IGNORE INTO tasks (course_id, section_id, task)
VALUES
    (
        1,
        1,
        "1. Go to the file explorer, press ENTER to activate it and create a directory called 'http_server.@
2. Inside that directory, create a file called 'answers.txt'.@
3. Switch to the editor (press ENTER to activate).@
4. In the editor, write down the three layers of the OSI model relevant to how an HTTP server operates.@@
Start with the highest layer and list for each:@
• The layer number (e.g. Layer 6),@
• The layer name (e.g. Presentation)@
• The protocol relevant to HTTP server implementation@@
Format your answer like this (comma-separated, one line per layer):@
Layer X, <name of layer>, <protocol>@@
When you completed the task, save the file by pressing F10, go back to the instructions window,
and press s to submit your solution."
    );
