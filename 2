INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "",
        "Great, now we have a working socket file descriptor and are ready to
move forward. The code of our server should now look similar to this (and,
again, no problemo if your version is 100% identical):",
        0,
        0
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "",
        '#define _POSIX_C_SOURCE 200112L@
#include <stdio.h>@
#include <string.h>@
#include <stdlib.h>@
#include <netdb.h>@
#include <errno.h>@
@
int main(void)@
{@
    struct addrinfo hints, *res;@
    int status, sockfd;@
@
    memset(&hints, 0, sizeof(hints));@
    hints.ai_family = AF_UNSPEC;@
    hints.ai_socktype = SOCK_STREAM;@
    hints.ai_flags = AI_PASSIVE;@
@
    if ((status = getaddrinfo(NULL, "8080", &hints, &res)) != 0)@
    {@
        fprintf(stderr, "getaddrinfo error: %s\n", gai_strerror(status));@
        exit(1);@
    }@
@
    if ((sockfd = socket(res->ai_family, res->ai_socktype, res->ai_protocol)) == -1)@
    {@
        fprintf(stderr, "socket error: %s\n", strerror(errno));@
    }@
@
    freeaddrinfo(res);@
}',
        1,
        1
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "",
        "We can now use the socket file descriptor provided by the socket
syscall as its return value to bind the socket to a specific port (which means,
we're operating on layer 4 in the OSI model now).",
        2,
        0
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "",
        "Usually you do this when you're building a server because you want a
server to use a specific port so it can be addressed by clients without problem.
When you're writing a client, it's not necessary, as in this case it's not
important which local port you use. You just call `connect()` without calling
`bind()` first, and the port is assigned automatically to your client socket.
So let's get back to bind().@",
        3,
        0
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "Historical sketch of the bind() syscall",
        "@The bind() syscall was introduced alongside the socket API in 4.2BSD
(1983), as part of the original design to make networking behave like file I/O.
",
        4,
        0
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "",
        "The idea was to let a program associate a socket with a specific local address
(IP and port), just like you might associate a file descriptor with a file path
using open(). This made it easier to write daemons or servers that listen for
incoming connections — a foundational concept in modern networking.@",
        5,
        0
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "Implementation of bind()",
        "@The call to bind is actually quite easy to implement. This is what
its prototype looks like:",
        6,
        0
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "Implementation of bind()",
        "@The call to bind is actually fairly easy to implement. This is what
its prototype looks like:",
        6,
        0
    );

INSERT
    OR IGNORE INTO materials (
        course_id,
        section_id,
        content_title,
        content,
        order_num,
        syntax_highlighting
    )
VALUES
    (
        1,
        4,
        "",
        "@#include <sys/types.h>@
#include <sys/socket.h>@
@
int bind(int sockfd, struct sockaddr *my_addr, int addrlen);@",
        6,
        0
    );
