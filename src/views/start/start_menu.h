#include "../../models/models.h"
#include <ncurses.h>
#include <menu.h>

MENU *create_start_menu(WINDOW *nav_window, APP_CONTEXT *ctx);
// void listen_for_menu_input(WINDOW **start_menu_win, MENU **start_menu);

